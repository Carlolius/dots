# Programs that autostart when Sway loads.
# Dunst
exec sleep 5 && dunst & 
# Megasync for Mega.
exec sleep 10 && exec megasync &
# Dropbox
exec sleep 10 && exec dropbox &
# KDE polkit, for graphical authentication.
exec --no-startup-id /usr/lib/polkit-kde-authentication-agent-1 &
